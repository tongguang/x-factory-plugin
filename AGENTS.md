# 开发约定

- 使用中文沟通。
- 保持实现简单，不做过度设计或防御。
- 插件放在 `plugins/<name>`，并在根 marketplace 中登记。
- 所有插件的用户配置统一放在 `~/.factory/plugin-config/<插件名>/`。
- 从仓库根目录运行 `npm ci`、`npm test`。
- 修改生图插件源码后，提交同步构建的 `dist/image-gen.cjs`。
- 测试使用模拟图片服务，不调用真实付费接口。
- 不提交真实配置、密钥、请求文件或生成图片。
